#!/usr/bin/env bash
set -eo pipefail

set +u
if [ -f .rvmrc ]; then
    source .rvmrc
elif [ -f .rvmrc.example ]; then
    source .rvmrc.example
else
    red_echo "Could not load .rvmrc or .rvmrc.example"
    exit 1
fi

# rvm gemset --force empty
echo "Rvm loaded"
set -u
